import {a} from './near';
import * as event from 'event';
import { a as b } from './luam'

print(a);
print(b);

([[1], [2], [3]]).flat().forEach(x => print(x));
print(event.d);

print(`${a} ${b} ${event.d}`)